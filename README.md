# Gamehub - A Game Library

**Project Name:** Gamehub - A Game Library  
**Live URL:** [Add your live URL here]  
**Purpose:**  
Gamehub is a React-based game library platform that allows users to explore, browse, and discover games easily. The project aims to provide a sleek, responsive, and interactive interface for gamers to check out game details, ratings, and more.

---

## Key Features

- **User Authentication:** Sign up and login using Firebase authentication.
- **Interactive UI:** Responsive design built with Tailwind CSS and DaisyUI.
- **Game Library Display:** Browse and search games with smooth navigation.
- **Sliders & Carousels:** Showcase featured games using Swiper.js.
- **Notifications:** Real-time notifications using React Toastify.
- **Routing:** Dynamic navigation with React Router.

---

## NPM Packages Used

### Dependencies
- `react` – Core React library
- `react-dom` – DOM rendering for React
- `react-router` – Routing library for navigation
- `firebase` – Backend and authentication services
- `tailwindcss` – Utility-first CSS framework
- `@tailwindcss/vite` – Tailwind plugin for Vite
- `react-icons` – Popular icons library for React
- `swiper` – Modern slider/carousel for React
- `react-toastify` – Notification library for React

### DevDependencies
- `vite` – Frontend build tool
- `@vitejs/plugin-react` – Vite plugin for React
- `daisyui` – Tailwind CSS component library
- `eslint` & `@eslint/js` – Code linting tools
- `eslint-plugin-react-hooks` – Lint rules for React hooks
- `eslint-plugin-react-refresh` – Lint support for React Fast Refresh
- `@types/react` & `@types/react-dom` – Type definitions for TypeScript (if used)
- `globals` – Global variables definitions

---
